import { CountAdvStatsPipe } from './count-adv-stats.pipe';

describe('CountAdvStatsPipe', () => {
  it('create an instance', () => {
    const pipe = new CountAdvStatsPipe();
    expect(pipe).toBeTruthy();
  });
});
